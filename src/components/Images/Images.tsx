import styled from 'styled-components'

const Icon = styled.img`
    height: auto;
    width: 1rem;
`

const Image = styled.img`
    border-radius: 0.5rem;
    display: block;
    width: 100%;
`

export { Icon, Image }